GEMINI_API_KEY=your_key_here
# Second, genuinely separate Google AI Studio project's key - used only by
# src/tier_comparison_runner.py (Groq/Gemini tier-tagging comparison), never
# by the production /query path. Keeping this on a different project means
# its daily quota use never competes with GEMINI_API_KEY's.
GEMINI_API_KEY_II=your_second_project_key_here
# Groq API key - used only by src/tier_comparison_runner.py, never by the
# production /query path.
GROQ_API_KEY=your_groq_key_here
